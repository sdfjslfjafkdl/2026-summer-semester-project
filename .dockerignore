# 비밀값
.env

# 버전관리·개발환경
.git
.gitignore
.venv
uv.lock

# 테스트·평가 (런타임에 필요 없다)
tests
evals
.pytest_cache
.ruff_cache

# 캐시·부산물
__pycache__
**/__pycache__
*.py[cod]
.DS_Store
**/.DS_Store

# 런타임에 생성되는 것들. 이미지에 굽지 않는다.
#   data/index    : 로컬 개발용 근거 검색 캐시 (컨테이너는 data/runtime/index 를 쓴다)
#   data/artifacts: 빌드 스테이지에서 scripts/build_artifacts.py 가 새로 만든다
data/index
data/runtime
data/artifacts

# 컨테이너 정의 자체
Dockerfile
.dockerignore
docker-compose.yml
railway.json
